# Problem: F_Dangerous_Sugoroku.pas

```pascal
program F_Dangerous_Sugoroku;
uses
    math;
const
    mm = 20 * 1000;

var
    n: int64;
    m, i: int32;
    a, b: int8;
    l, r: array [1 .. mm] of int64;

begin
    readln(n, m, a, b);
    for i := 1 to m do read(l[i], r[i]);
end.

```
