# Problem: C_Swap_Characters.pas

```pascal
program C_Swap_Characters;
const
    maxn = 250 * 1000;
    prime = 998244353;
var
    n, k, i: int32;
    s: array [1 .. maxn] of char;

begin
    readln(n, k);

    for i := 1 to n do begin
        read(s[i]);
    end;
    readln;

end.

```
