# Problem: E_Clique_Connect.pas

```pascal
program E_Clique_Connect;
uses
    math;
const
    nn = 200 * 1000;
var
    n, k, i: int32;
    p, inv: array [1 .. nn] of int32;

begin
    readln(n, k);

    for i := 1 to n do begin
        read(p[i]);
    end;
    readln;

end.

```
