# Problem: E.pas

```pascal
program _E;
uses
	math;
const
	nn = 3000;
var
	notc, tci, n, i: int32;
	ans: int64;
	s: array [1 .. nn] of string;

begin
	readln(notc);
	for tci := 1 to notc do begin

		readln(n);
		writeln(ans);

	end;
end.

```
