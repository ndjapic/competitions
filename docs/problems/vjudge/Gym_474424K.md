# Problem: Gym_474424K.pas

```pascal
program Gym_474424K;
var
    c: char;
    a, d: int8;

begin
    d := ord('a') - ord('A');
    readln(c);
    a := ord(c);
    if c <= 'Z' then
        inc(a, d)
    else
        dec(a, d);
    c := chr(a);
    writeln(c);
end.

```
