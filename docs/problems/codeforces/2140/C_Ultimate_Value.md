# Problem: C_Ultimate_Value.pas

```pascal
program C_Ultimate_Value;
const
	nn = 200 * 1000;
var
	ntc, tci, n, i: int32;

function alice(): boolean;
var
	l, r: int32;
begin
	l := 0;
end;

begin
	readln(ntc);
	for tci := 1 to ntc do begin

		readln(n);
		for i := 1 to n do read(a[i]); readln;

	end;
end.

```
