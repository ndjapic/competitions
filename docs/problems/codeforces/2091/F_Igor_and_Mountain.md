# Problem: F_Igor_and_Mountain.pas

```pascal
program F_Igor_and_Mountain;
const
    nn = 10 * 1000 * 1000;
var
    ntc, tci: int16;
    n, p, i: int32;
    primes: array [1 .. pc] of int32;

begin
    readln(ntc);
    for tci := 1 to ntc do begin

        readln(n);

        writeln(ans);

    end;
end.

```
