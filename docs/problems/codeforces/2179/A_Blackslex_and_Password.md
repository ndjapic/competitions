# Problem: A_Blackslex_and_Password.pas

```pascal
program A_Blackslex_and_Password;
var
	notc, tci, k, x: int32;

begin
	readln(notc);
	for tci := 1 to notc do begin

		readln(k, x);
		writeln(k*x+1);

	end;
end.

```
