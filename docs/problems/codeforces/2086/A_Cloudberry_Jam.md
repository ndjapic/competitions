# Problem: A_Cloudberry_Jam.pas

```pascal
program A_Cloudberry_Jam;
var
    ntc, tci: int16;
    n: int32;

begin
    readln(ntc);
    for tci := 1 to ntc do begin

        readln(n);
        writeln(2*n);

    end;
end.

```
