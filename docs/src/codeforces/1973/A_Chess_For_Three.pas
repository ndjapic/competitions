program C_Cat_Fox_and_Double_Maximum;
const
	nn = 100 * 1000;
var
    ntc, tci: int16;
    n, i: int32;
    p, inv, q, a: array [1 .. nn] of int32;
    seen: array [1 .. nn] of boolean;

begin
    readln(ntc);
    for tci := 1 to ntc do begin

		readln(n);
		for i := 1 to n do begin
			read(p[i]);
			q[i] := n+1-p[i];
			if inv[p[i]] := i;
		end;
		readln;

		if inv[n] = n then

    end;
end.
