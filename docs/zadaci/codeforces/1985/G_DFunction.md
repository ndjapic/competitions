# Задатак: G_DFunction.pas

```pascal
program G_DFunction;
{$mode objfpc}{$H+}{$J-}
const
    prime = 1000 * 1000 * 1000 + 7;
var
    ntc, tci: int16;
    l, r, k: int32;
    a: array [1 .. nn] of int32;

begin
    readln(ntc);
    for tci := 1 to ntc do begin

        readln(l, r, k);

    end;
end.

```
