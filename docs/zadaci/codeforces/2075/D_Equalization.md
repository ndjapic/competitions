# Задатак: D_Equalization.pas

```pascal
program D_Equalization;
{$MODE DELPHI}
uses
    math;
const
    nn = 200 * 1000;
var
    ntc, tci: int16;
    n, m, i, j, ai: int32;
    s: array [0 .. nn] of int64;

begin
    readln(ntc);
    for tci := 1 to ntc do begin

    end;

end.

```
