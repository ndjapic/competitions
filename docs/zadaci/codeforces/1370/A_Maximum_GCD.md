# Задатак: A_Maximum_GCD.pas

```pascal
program A_Maximum_GCD;
var
    ntc, tci, n: int32;

begin
    readln(ntc);
    for tci := 1 to ntc do begin

        readln(n);
        writeln(n div 2);

    end;
end.

```
