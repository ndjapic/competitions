# Задатак: F_Alex_s_whims.pas

```pascal
program F_Alex_s_whims;
uses
    math;
const
    maxn = 500;
var
    ntc, tci: int8;
    n, i: int16;
    d: array [1 .. maxn] of int16;

begin
    readln(ntc);
    for tci := 1 to ntc do begin

        readln(n);

        for i := 1 to n do begin
            readln(d[i]);
        end;
        readln;

        writeln(m-1);

    end;
end.

```
