# Задатак: D_Avoid_K_Palindrome.pas

```pascal
program D_Avoid_K_Palindrome;
{$mode objfpc}{$H+}{$J-}
uses
    math;
const
    prime = 998244353;
var
    n, k, i: int16;
    s: string;

begin
    readln(n, k);
    readln(s);

    writeln(ans);
end.

```
