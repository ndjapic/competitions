# Задатак: C_Distance_Indicators.pas

```pascal
program C_Distance_Indicators;
const
	nn = 200 * 1000;
var
	n, i: int8;
	a: array [1 .. nn] of int32;

begin
	readln(n);

	for i := 1 to n do read(a[i]); readln;
end.

```
