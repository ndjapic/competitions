# Задатак: E_Reverse_2_i.pas

```pascal
program E_Reverse_2_i;
uses
    math;
const
    nn = 256 * 1024;
var
    ntc, tci, n, i: int32;
    p: array [1 .. nn] of int32;

begin
    readln(ntc);
    for tci := 1 to ntc do begin

        readln(n);
        for i := 1 to n do read(a[i]); readln;

    end;
end.

```
